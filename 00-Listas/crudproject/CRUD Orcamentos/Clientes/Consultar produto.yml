info:
  name: Consultar produto
  type: http

http:
  method: GET
  url: http://localhost:8080/produtos
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
