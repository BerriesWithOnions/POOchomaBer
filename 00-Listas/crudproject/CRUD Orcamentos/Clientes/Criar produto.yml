info:
  name: Criar produto
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/produtos
  body:
    type: json
    data: |-
      {
        "nome": "Notebook",
        "preco": 3500.00
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
