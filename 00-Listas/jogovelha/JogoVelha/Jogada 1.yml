info:
  name: Jogada 1
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/partidas/1/jogadas?simbolo=X&linha=0&coluna=1
  params:
    - name: simbolo
      value: X
      type: query
    - name: linha
      value: "0"
      type: query
    - name: coluna
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
